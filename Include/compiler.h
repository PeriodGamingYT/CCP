#include "values.h"
#include "numbers.h"
#include <stdio.h>
#include <string.h>
int compileLine(char line[]) {
int i, charLong, charStart;
char *command, *value, *option;
	while(line[i] != '\n' || line[i] != ';') {

	} // Will come back later.
}
